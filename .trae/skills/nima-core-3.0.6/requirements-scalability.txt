# NIMA Scalability Dependencies
# Install with: pip install -r requirements-scalability.txt

# FAISS for O(log N) vector search
# Use faiss-cpu for most systems, faiss-gpu for CUDA systems
faiss-cpu>=1.7.4

# Core dependencies (likely already installed)
numpy>=1.24.0
voyageai>=0.2.0
